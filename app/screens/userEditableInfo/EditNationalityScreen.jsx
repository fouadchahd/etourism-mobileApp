import React from "react";
import { StyleSheet, Text, View } from "react-native";
import palettes from "res/palettes";
const EditNationalityScreen = () => {
  return (
    <View style={[palettes.screen, { backgroundColor: "white" }]}>
      <Text>EditNationalityScreen</Text>
    </View>
  );
};

export default EditNationalityScreen;

const styles = StyleSheet.create({});
